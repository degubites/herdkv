# Editor Viewer

Open `Window > HerdKV > Viewer` in the Unity Editor.

The viewer can:

- open a HerdKV database folder
- list live keys
- search keys
- preview UTF-8 or hex values
- export selected values
- delete keys
- compact a database
- show corruption warnings from inspection
