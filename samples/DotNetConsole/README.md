# HerdKV .NET Console Sample

Run from the repository root:

```text
dotnet run --project samples/DotNetConsole/HerdKV.DotNetConsoleSample.csproj
```

The sample writes and reads typed values using the Core package outside Unity.
